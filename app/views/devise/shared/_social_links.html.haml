- type ||= nil
- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to t("#{type || "Log in"} with #{provider.to_s.titleize}"), omniauth_authorize_path(resource_name, provider), rel: :nofollow
    %br/
