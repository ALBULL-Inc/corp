%h1 Listing accounts

%table.table
  %thead
    %tr
      %th ID
      %th 姓
      %th 名
      %th Email
      %th ニックネーム
      %th 性別
      %th ログイン回数
      %th 作成日
      %th

  %tbody
    - @accounts.each do |account|
      %tr
        %td= account.id
        %td= account.lastname
        %td= account.firstname
        %td= account.email
        %td= account.nickname
        %td= gender2str account.gender
        %td= account.sign_in_count
        %td=l account.created_at
        %td= link_to 'Show', [:cms,account]

.t-center
  = paginate @accounts
