- set_meta_tags site: "ダーツするならレイリーで | ダーツバーレイリー 赤坂店 #{t("site.name")}", canonical: root_url,
  description: "株式会社オルブルが運営するDartsBarRailly(ダーツバーレイリー)のサイトです。各店舗の情報やお得情報、サブスクリプション(月額会員)などをご利用いただけます！会員登録することでお得に各店舗をご利用いただけます。"

.eye-catch.mb20{style: "height: 240px"}
  .text-center.pt60.c-white
    %h1.mt0.mb20
      %span.f30 ダーツするなら
      %br
      %span.f30 レイリーで
    %p.lead
      Welcome to #{t('site.name')}

%p.text-center
  <!-- 横長1-1 -->
  = render partial: 'common/adsense', locals: {slot_id: 4877223498, w: 728, h: 90}

.row.mb50
  .col-md-12
    .panel.panel-default
      .panel-heading
        %h2.panel-title.f18 トピックス
      .panel-body
        %table.table
          - @topics.each do |topic|
            %tr
              %td.col-md-2=l topic.publish_at.to_date, format: :topic
              %td= link_to topic.title, topic
      .panel-footer.text-right
        = link_to 'more »', topics_path
  .col-md-12
    - unless Rails.env.production?
      .panel.panel-default
        .panel-body{style: "height: 260.5px"}
          260.5x260.5

.row
  .col-md-12
    %ul.list-menu
      %li= link_to "#{fa_icon 'store-alt'} 店舗情報".html_safe, stores_path

  - @stores.each do |store|
    .col-md-12
      .panel.panel-default
        .panel-heading
          %h2.panel-title.f18= store.name
        .panel-body
          %div.f14{style: 'padding: 10px;'}
            - if store.comming_soon?
              Comming soon...
            - else
              #{fa_icon 'map-marker'}
              = store.full_address
              %br
              #{fa_icon 'phone-square'}
              = store.tel
              %br
              #{fa_icon 'clock'}
              = store.opening_time
              \-
              = store.closing_time
        - unless store.comming_soon?
          .panel-footer.text-right
            = link_to 'more »', store

