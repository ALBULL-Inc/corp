- set_meta_tags title: "Darts Bar Railly #{@store.name}", canonical: store_url(@store),
  description: "株式会社オルブルが運営するDarts Bar Railly(レイリー)の#{@store.name}の情報やお得な情報を掲載しています。さらに会員登録することでお得に#{@store.name}をご利用いただけます。#{@store.near_station}。#{@store.spec}"

.row
  .col-md-2
  .col-md-8
    %h1= "Darts Bar Railly #{@store.name}"
    .row
      .col-xs-5
        %h2.f20 【最寄り駅】
        %p= bbr @store.near_station

        %h3.f20 【店内仕様】
        %p= bbr @store.spec

      .col-xs-7
        %p
          = fa_icon 'map-marker-alt'
          = @store.full_address

        %p.t-center
          %iframe(src="https://www.google.com/maps/embed?#{@store.gmap_query}" width="425" height="400" frameborder="0" style="border:0" allowfullscreen)

    %p.mt25
      = fa_icon 'phone-square'
      %span.f20= @store.tel
      \/
      = fa_icon 'print'
      %span.f16= @store.fax

    %p
      ご予約承ります!!上記までお電話ください！

    .row
      .col-xs-12
        %h3 メニュー
    .row
      - @menus.each do |m|
        .col-xs-4= link_to_if m.content.present?, m.name, m
        .col-xs-2= m.price

- content_for(:jsonld) do
  = jsonld_script_tag
