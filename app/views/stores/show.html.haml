- set_meta_tags title: "#{@store.name}", canonical: store_url(@store.pkey),
  description: "株式会社オルブルが運営する#{@store.name}の情報やお得な情報を掲載しています。#{@store.near_station}。#{@store.spec}",
  twitter: {card: "summary_large_image", site: "@#{@store.twitter_uid}", title: @store.name, image: asset_url("store/full/#{@store.id}.png")}

- content_for(:jsonld) do
  = jsonld_script_tag

.row
  %h1= @store.name
  .col-md-12
    %p
      = fa_icon 'phone-square'
      %span.f20= link_to @store.tel, "tel:#{@store.tel}"
      \/
      = fa_icon 'print'
      %span.f16= @store.fax
      %span.ml20 ご予約承ります!!上記までお電話ください！

    .row.mt50
      .col-md-5
        %h2.f20 【最寄り駅】
        %p= bbr @store.near_station

        %h3.f20 【店内仕様】
        %p= bbr @store.spec

      .col-md-7
        %p
          = fa_icon 'map-marker-alt'
          = @store.full_address

        %p
          %iframe(src="https://www.google.com/maps/embed?#{@store.gmap_query}" width="425" height="400" frameborder="0" style="border:0" allowfullscreen)

    .row
      .col-xs-12.mt20
        %h3 ドリンクメニュー
    .row
      - @menus.each do |m|
        .col-md-4= link_to_if m.content.present?, m.name, m
        .col-md-2= m.price
