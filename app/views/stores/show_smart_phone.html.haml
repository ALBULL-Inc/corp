- set_meta_tags title: @store.name, canonical: store_url(@store),
  description: "株式会社オルブルが運営するDarts Bar Railly(レイリー)の#{@store.name}の情報やお得な情報を掲載しています。さらに会員登録することでお得に#{@store.name}をご利用いただけます。#{@store.near_station}。#{@store.spec}"

- content_for(:jsonld) do
  = jsonld_script_tag

.row
  .col-12
    %h1.f28= @store.name
    %p.t-center
      %iframe(src="https://www.google.com/maps/embed?#{@store.gmap_query}" width="100%" height="250" frameborder="0" style="border:0" allowfullscreen)
    %h2.f20 【最寄り駅】
    %p= bbr @store.near_station

    %h3.f20 【店内仕様】
    %p= bbr @store.spec

    %p
      = fa_icon 'map-marker-alt'
      = @store.full_address


    %p.mt25
      = fa_icon 'phone-square'
      %span.f20= link_to @store.tel, "tel:#{@store.tel}"
      \/
      = fa_icon 'print'
      %span.f16= @store.fax

    %p
      ご予約承ります!!上記までお電話ください！

    .row
      .col-12
        %h3 メニュー
    .row
      - @menus.each do |m|
        .col-8
          .ml10= link_to_if m.content.present?, m.name, m
        .col-4.t-right
          .mr20= m.price

