- set_meta_tags title: @store.name, canonical: store_url(@store),
  description: "株式会社オルブルが運営するDartsBarRailly(ダーツバーレイリー)の#{@store.name}の情報やお得な情報を掲載しています。さらに会員登録することでお得に#{@store.name}をご利用いただけます。"

.row
  .col-md-2
  .col-md-8
    %h1= "Darts Bar Railly #{@store.name}"
    .row
      .col-md-12
        %p.t-center
          %iframe(src="https://www.google.com/maps/embed?#{@store.gmap_query}" width="100%" height="250" frameborder="0" style="border:0" allowfullscreen)
        %h2.f20 【店内仕様】
        %p= bbr @store.spec

        %p
          = fa_icon 'map-marker-alt'
          = @store.full_address


    %p.mt25
      = fa_icon 'phone-square'
      %span.f20= @store.tel
      \/
      = fa_icon 'print'
      %span.f16= @store.fax

    %p
      ご予約承ります!!上記までお電話ください！

    .row
      .col-xs-12
        %h3 メニュー
    .row
      - @menus.each do |m|
        .col-xs-8= link_to_if m.content.present?, m.name, m
        .col-xs-4= m.price

